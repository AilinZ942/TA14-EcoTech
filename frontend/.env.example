# Copy to .env.local and fill in values. .env.local is gitignored.
# Vite reads variables prefixed with VITE_.

# Public Mapbox token (pk.…). Used by DisposalLocations and PickupPoints.
# Get one at https://account.mapbox.com/access-tokens — DO NOT use sk.… tokens.
VITE_MAPBOX_ACCESS_TOKEN=
